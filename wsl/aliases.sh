#!/bin/bash

# Aliases that are only added if I'm using WSL

# alias cdd="cd $userprofile/Desktop"
# alias code="cd $userprofile/Desktop/coalpha"
alias clip="$system32/clip.exe"
alias attrib="$system32/attrib.exe"
alias vscode="$C/Program Files/Microsoft VS Code/bin/code"
